# Supabase project backing the site's content (books, blog, news,
# testimonials), the /admin CMS, the newsletter form, and the contact form.
# Find these under Project Settings > API in the Supabase dashboard.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
