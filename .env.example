# POST endpoint for the newsletter form (see src/components/NewsletterForm.tsx).
# Point this at a MailerLite/ConvertKit (or similar) form endpoint that accepts
# a JSON POST of { "email": "..." }. Leave unset during local development to
# have the form simulate success without making a network request.
VITE_NEWSLETTER_ENDPOINT=
